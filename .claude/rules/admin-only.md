# Admin Only

This rule set applies to private admin surfaces only.

Requirements:
- Never expose admin prompts or internal audits to public endpoints.
- Validate admin auth and authorization server-side.
- Keep admin caches isolated from public caches.
