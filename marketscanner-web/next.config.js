/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Add your Replit dev origin here (optional; silences the dev warning)
    // undefined'https://YOUR-REPLIT-PREVIEW-URL.replit.dev'],
  },
};

module.exports = nextConfig;
