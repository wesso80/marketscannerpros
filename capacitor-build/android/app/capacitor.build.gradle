// Capacitor build configuration
android {
  compileSdkVersion 34
  
  defaultConfig {
    minSdkVersion 22
    targetSdkVersion 34
  }
}

dependencies {
  // Capacitor dependencies will be added here
}