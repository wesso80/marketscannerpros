export const metadata = { title: "Dashboard — MarketScanner Pros" };

export default function Dashboard() {
  return (
    <main>
      <h1>Dashboard</h1>
      <p>Signed-in users will see their scans, watchlists and alerts here.</p>
      <p>(We’ll wire auth later.)</p>
    </main>
  );
}
